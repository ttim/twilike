<table id='timelines' cellpadding='0' cellspacing='0'>
  <tr>
    <% [:plus, :neutral, :minus].each do |rate| %>
      <td class='timeline' valign='top'>
        <% @timelines[rate].each do |opinion| %>
          <%= render_cell :opinion, :for_timeline, :opinion => opinion %>
        <% end %>
      </td>
    <% end %>
  </tr>
</table>
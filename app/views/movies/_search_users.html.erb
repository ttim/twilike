<% if @users.size == 0 %>
    <%= t 'search.nothing_found' %>
<% end %>

<% @users.each do |user| %>
    <%= render_cell 'user', 'for_top', :time_interval => nil, :user => user %>
<% end %>

<%= render :partial => 'common/pagination', :locals => {:collection => @users} %>